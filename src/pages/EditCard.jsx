import { useParams } from "react-router-dom";
import { All } from "../assets/styled/editCardSryled/editCardSryled";

export function EditCard() {
    const { id } = useParams();


    return (
        <All>
           
        </All>
    )
}